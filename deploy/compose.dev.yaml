services:
  sub2api:
    image: sub2api-dev
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      SERVER_MODE: debug
    # Compose v2.24.4+ replaces the canonical public mapping instead of appending.
    ports: !override
      - "127.0.0.1:${PUBLISH_PORT:-8080}:8080"
