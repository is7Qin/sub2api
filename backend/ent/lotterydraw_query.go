// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/lotterycampaign"
	"github.com/Wei-Shaw/sub2api/ent/lotterychance"
	"github.com/Wei-Shaw/sub2api/ent/lotterydraw"
	"github.com/Wei-Shaw/sub2api/ent/lotteryprize"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/redeemcode"
	"github.com/Wei-Shaw/sub2api/ent/user"
)

// LotteryDrawQuery is the builder for querying LotteryDraw entities.
type LotteryDrawQuery struct {
	config
	ctx                  *QueryContext
	order                []lotterydraw.OrderOption
	inters               []Interceptor
	predicates           []predicate.LotteryDraw
	withCampaign         *LotteryCampaignQuery
	withUser             *UserQuery
	withChance           *LotteryChanceQuery
	withPrize            *LotteryPrizeQuery
	withRedeemCodeEntity *RedeemCodeQuery
	modifiers            []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LotteryDrawQuery builder.
func (_q *LotteryDrawQuery) Where(ps ...predicate.LotteryDraw) *LotteryDrawQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *LotteryDrawQuery) Limit(limit int) *LotteryDrawQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *LotteryDrawQuery) Offset(offset int) *LotteryDrawQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *LotteryDrawQuery) Unique(unique bool) *LotteryDrawQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *LotteryDrawQuery) Order(o ...lotterydraw.OrderOption) *LotteryDrawQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCampaign chains the current query on the "campaign" edge.
func (_q *LotteryDrawQuery) QueryCampaign() *LotteryCampaignQuery {
	query := (&LotteryCampaignClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotterydraw.Table, lotterydraw.FieldID, selector),
			sqlgraph.To(lotterycampaign.Table, lotterycampaign.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lotterydraw.CampaignTable, lotterydraw.CampaignColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (_q *LotteryDrawQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotterydraw.Table, lotterydraw.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lotterydraw.UserTable, lotterydraw.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChance chains the current query on the "chance" edge.
func (_q *LotteryDrawQuery) QueryChance() *LotteryChanceQuery {
	query := (&LotteryChanceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotterydraw.Table, lotterydraw.FieldID, selector),
			sqlgraph.To(lotterychance.Table, lotterychance.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, lotterydraw.ChanceTable, lotterydraw.ChanceColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPrize chains the current query on the "prize" edge.
func (_q *LotteryDrawQuery) QueryPrize() *LotteryPrizeQuery {
	query := (&LotteryPrizeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotterydraw.Table, lotterydraw.FieldID, selector),
			sqlgraph.To(lotteryprize.Table, lotteryprize.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lotterydraw.PrizeTable, lotterydraw.PrizeColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRedeemCodeEntity chains the current query on the "redeem_code_entity" edge.
func (_q *LotteryDrawQuery) QueryRedeemCodeEntity() *RedeemCodeQuery {
	query := (&RedeemCodeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(lotterydraw.Table, lotterydraw.FieldID, selector),
			sqlgraph.To(redeemcode.Table, redeemcode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lotterydraw.RedeemCodeEntityTable, lotterydraw.RedeemCodeEntityColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LotteryDraw entity from the query.
// Returns a *NotFoundError when no LotteryDraw was found.
func (_q *LotteryDrawQuery) First(ctx context.Context) (*LotteryDraw, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{lotterydraw.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *LotteryDrawQuery) FirstX(ctx context.Context) *LotteryDraw {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LotteryDraw ID from the query.
// Returns a *NotFoundError when no LotteryDraw ID was found.
func (_q *LotteryDrawQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{lotterydraw.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *LotteryDrawQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LotteryDraw entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LotteryDraw entity is found.
// Returns a *NotFoundError when no LotteryDraw entities are found.
func (_q *LotteryDrawQuery) Only(ctx context.Context) (*LotteryDraw, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{lotterydraw.Label}
	default:
		return nil, &NotSingularError{lotterydraw.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *LotteryDrawQuery) OnlyX(ctx context.Context) *LotteryDraw {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LotteryDraw ID in the query.
// Returns a *NotSingularError when more than one LotteryDraw ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *LotteryDrawQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{lotterydraw.Label}
	default:
		err = &NotSingularError{lotterydraw.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *LotteryDrawQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LotteryDraws.
func (_q *LotteryDrawQuery) All(ctx context.Context) ([]*LotteryDraw, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LotteryDraw, *LotteryDrawQuery]()
	return withInterceptors[[]*LotteryDraw](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *LotteryDrawQuery) AllX(ctx context.Context) []*LotteryDraw {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LotteryDraw IDs.
func (_q *LotteryDrawQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(lotterydraw.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *LotteryDrawQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *LotteryDrawQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*LotteryDrawQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *LotteryDrawQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *LotteryDrawQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *LotteryDrawQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LotteryDrawQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *LotteryDrawQuery) Clone() *LotteryDrawQuery {
	if _q == nil {
		return nil
	}
	return &LotteryDrawQuery{
		config:               _q.config,
		ctx:                  _q.ctx.Clone(),
		order:                append([]lotterydraw.OrderOption{}, _q.order...),
		inters:               append([]Interceptor{}, _q.inters...),
		predicates:           append([]predicate.LotteryDraw{}, _q.predicates...),
		withCampaign:         _q.withCampaign.Clone(),
		withUser:             _q.withUser.Clone(),
		withChance:           _q.withChance.Clone(),
		withPrize:            _q.withPrize.Clone(),
		withRedeemCodeEntity: _q.withRedeemCodeEntity.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCampaign tells the query-builder to eager-load the nodes that are connected to
// the "campaign" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotteryDrawQuery) WithCampaign(opts ...func(*LotteryCampaignQuery)) *LotteryDrawQuery {
	query := (&LotteryCampaignClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCampaign = query
	return _q
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotteryDrawQuery) WithUser(opts ...func(*UserQuery)) *LotteryDrawQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithChance tells the query-builder to eager-load the nodes that are connected to
// the "chance" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotteryDrawQuery) WithChance(opts ...func(*LotteryChanceQuery)) *LotteryDrawQuery {
	query := (&LotteryChanceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChance = query
	return _q
}

// WithPrize tells the query-builder to eager-load the nodes that are connected to
// the "prize" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotteryDrawQuery) WithPrize(opts ...func(*LotteryPrizeQuery)) *LotteryDrawQuery {
	query := (&LotteryPrizeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPrize = query
	return _q
}

// WithRedeemCodeEntity tells the query-builder to eager-load the nodes that are connected to
// the "redeem_code_entity" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *LotteryDrawQuery) WithRedeemCodeEntity(opts ...func(*RedeemCodeQuery)) *LotteryDrawQuery {
	query := (&RedeemCodeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRedeemCodeEntity = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CampaignID int64 `json:"campaign_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LotteryDraw.Query().
//		GroupBy(lotterydraw.FieldCampaignID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *LotteryDrawQuery) GroupBy(field string, fields ...string) *LotteryDrawGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LotteryDrawGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = lotterydraw.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CampaignID int64 `json:"campaign_id,omitempty"`
//	}
//
//	client.LotteryDraw.Query().
//		Select(lotterydraw.FieldCampaignID).
//		Scan(ctx, &v)
func (_q *LotteryDrawQuery) Select(fields ...string) *LotteryDrawSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &LotteryDrawSelect{LotteryDrawQuery: _q}
	sbuild.label = lotterydraw.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LotteryDrawSelect configured with the given aggregations.
func (_q *LotteryDrawQuery) Aggregate(fns ...AggregateFunc) *LotteryDrawSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *LotteryDrawQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !lotterydraw.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *LotteryDrawQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LotteryDraw, error) {
	var (
		nodes       = []*LotteryDraw{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withCampaign != nil,
			_q.withUser != nil,
			_q.withChance != nil,
			_q.withPrize != nil,
			_q.withRedeemCodeEntity != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LotteryDraw).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LotteryDraw{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCampaign; query != nil {
		if err := _q.loadCampaign(ctx, query, nodes, nil,
			func(n *LotteryDraw, e *LotteryCampaign) { n.Edges.Campaign = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *LotteryDraw, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChance; query != nil {
		if err := _q.loadChance(ctx, query, nodes, nil,
			func(n *LotteryDraw, e *LotteryChance) { n.Edges.Chance = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPrize; query != nil {
		if err := _q.loadPrize(ctx, query, nodes, nil,
			func(n *LotteryDraw, e *LotteryPrize) { n.Edges.Prize = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRedeemCodeEntity; query != nil {
		if err := _q.loadRedeemCodeEntity(ctx, query, nodes, nil,
			func(n *LotteryDraw, e *RedeemCode) { n.Edges.RedeemCodeEntity = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *LotteryDrawQuery) loadCampaign(ctx context.Context, query *LotteryCampaignQuery, nodes []*LotteryDraw, init func(*LotteryDraw), assign func(*LotteryDraw, *LotteryCampaign)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LotteryDraw)
	for i := range nodes {
		fk := nodes[i].CampaignID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lotterycampaign.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "campaign_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *LotteryDrawQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*LotteryDraw, init func(*LotteryDraw), assign func(*LotteryDraw, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LotteryDraw)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *LotteryDrawQuery) loadChance(ctx context.Context, query *LotteryChanceQuery, nodes []*LotteryDraw, init func(*LotteryDraw), assign func(*LotteryDraw, *LotteryChance)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LotteryDraw)
	for i := range nodes {
		fk := nodes[i].ChanceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lotterychance.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "chance_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *LotteryDrawQuery) loadPrize(ctx context.Context, query *LotteryPrizeQuery, nodes []*LotteryDraw, init func(*LotteryDraw), assign func(*LotteryDraw, *LotteryPrize)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LotteryDraw)
	for i := range nodes {
		if nodes[i].PrizeID == nil {
			continue
		}
		fk := *nodes[i].PrizeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lotteryprize.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "prize_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *LotteryDrawQuery) loadRedeemCodeEntity(ctx context.Context, query *RedeemCodeQuery, nodes []*LotteryDraw, init func(*LotteryDraw), assign func(*LotteryDraw, *RedeemCode)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*LotteryDraw)
	for i := range nodes {
		if nodes[i].RedeemCodeID == nil {
			continue
		}
		fk := *nodes[i].RedeemCodeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(redeemcode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "redeem_code_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *LotteryDrawQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *LotteryDrawQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(lotterydraw.Table, lotterydraw.Columns, sqlgraph.NewFieldSpec(lotterydraw.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, lotterydraw.FieldID)
		for i := range fields {
			if fields[i] != lotterydraw.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCampaign != nil {
			_spec.Node.AddColumnOnce(lotterydraw.FieldCampaignID)
		}
		if _q.withUser != nil {
			_spec.Node.AddColumnOnce(lotterydraw.FieldUserID)
		}
		if _q.withChance != nil {
			_spec.Node.AddColumnOnce(lotterydraw.FieldChanceID)
		}
		if _q.withPrize != nil {
			_spec.Node.AddColumnOnce(lotterydraw.FieldPrizeID)
		}
		if _q.withRedeemCodeEntity != nil {
			_spec.Node.AddColumnOnce(lotterydraw.FieldRedeemCodeID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *LotteryDrawQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(lotterydraw.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = lotterydraw.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *LotteryDrawQuery) ForUpdate(opts ...sql.LockOption) *LotteryDrawQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *LotteryDrawQuery) ForShare(opts ...sql.LockOption) *LotteryDrawQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// LotteryDrawGroupBy is the group-by builder for LotteryDraw entities.
type LotteryDrawGroupBy struct {
	selector
	build *LotteryDrawQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *LotteryDrawGroupBy) Aggregate(fns ...AggregateFunc) *LotteryDrawGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *LotteryDrawGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotteryDrawQuery, *LotteryDrawGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *LotteryDrawGroupBy) sqlScan(ctx context.Context, root *LotteryDrawQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LotteryDrawSelect is the builder for selecting fields of LotteryDraw entities.
type LotteryDrawSelect struct {
	*LotteryDrawQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *LotteryDrawSelect) Aggregate(fns ...AggregateFunc) *LotteryDrawSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *LotteryDrawSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LotteryDrawQuery, *LotteryDrawSelect](ctx, _s.LotteryDrawQuery, _s, _s.inters, v)
}

func (_s *LotteryDrawSelect) sqlScan(ctx context.Context, root *LotteryDrawQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
