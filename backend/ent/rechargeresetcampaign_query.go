// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
	"github.com/Wei-Shaw/sub2api/ent/rechargeresetcampaign"
	"github.com/Wei-Shaw/sub2api/ent/rechargeresetcampaignrule"
	"github.com/Wei-Shaw/sub2api/ent/rechargeresetrecord"
)

// RechargeResetCampaignQuery is the builder for querying RechargeResetCampaign entities.
type RechargeResetCampaignQuery struct {
	config
	ctx         *QueryContext
	order       []rechargeresetcampaign.OrderOption
	inters      []Interceptor
	predicates  []predicate.RechargeResetCampaign
	withRules   *RechargeResetCampaignRuleQuery
	withRecords *RechargeResetRecordQuery
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RechargeResetCampaignQuery builder.
func (_q *RechargeResetCampaignQuery) Where(ps ...predicate.RechargeResetCampaign) *RechargeResetCampaignQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RechargeResetCampaignQuery) Limit(limit int) *RechargeResetCampaignQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RechargeResetCampaignQuery) Offset(offset int) *RechargeResetCampaignQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RechargeResetCampaignQuery) Unique(unique bool) *RechargeResetCampaignQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RechargeResetCampaignQuery) Order(o ...rechargeresetcampaign.OrderOption) *RechargeResetCampaignQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryRules chains the current query on the "rules" edge.
func (_q *RechargeResetCampaignQuery) QueryRules() *RechargeResetCampaignRuleQuery {
	query := (&RechargeResetCampaignRuleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rechargeresetcampaign.Table, rechargeresetcampaign.FieldID, selector),
			sqlgraph.To(rechargeresetcampaignrule.Table, rechargeresetcampaignrule.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, rechargeresetcampaign.RulesTable, rechargeresetcampaign.RulesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRecords chains the current query on the "records" edge.
func (_q *RechargeResetCampaignQuery) QueryRecords() *RechargeResetRecordQuery {
	query := (&RechargeResetRecordClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(rechargeresetcampaign.Table, rechargeresetcampaign.FieldID, selector),
			sqlgraph.To(rechargeresetrecord.Table, rechargeresetrecord.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, rechargeresetcampaign.RecordsTable, rechargeresetcampaign.RecordsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RechargeResetCampaign entity from the query.
// Returns a *NotFoundError when no RechargeResetCampaign was found.
func (_q *RechargeResetCampaignQuery) First(ctx context.Context) (*RechargeResetCampaign, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rechargeresetcampaign.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) FirstX(ctx context.Context) *RechargeResetCampaign {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RechargeResetCampaign ID from the query.
// Returns a *NotFoundError when no RechargeResetCampaign ID was found.
func (_q *RechargeResetCampaignQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rechargeresetcampaign.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RechargeResetCampaign entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RechargeResetCampaign entity is found.
// Returns a *NotFoundError when no RechargeResetCampaign entities are found.
func (_q *RechargeResetCampaignQuery) Only(ctx context.Context) (*RechargeResetCampaign, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rechargeresetcampaign.Label}
	default:
		return nil, &NotSingularError{rechargeresetcampaign.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) OnlyX(ctx context.Context) *RechargeResetCampaign {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RechargeResetCampaign ID in the query.
// Returns a *NotSingularError when more than one RechargeResetCampaign ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RechargeResetCampaignQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rechargeresetcampaign.Label}
	default:
		err = &NotSingularError{rechargeresetcampaign.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RechargeResetCampaigns.
func (_q *RechargeResetCampaignQuery) All(ctx context.Context) ([]*RechargeResetCampaign, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RechargeResetCampaign, *RechargeResetCampaignQuery]()
	return withInterceptors[[]*RechargeResetCampaign](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) AllX(ctx context.Context) []*RechargeResetCampaign {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RechargeResetCampaign IDs.
func (_q *RechargeResetCampaignQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(rechargeresetcampaign.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RechargeResetCampaignQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RechargeResetCampaignQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RechargeResetCampaignQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RechargeResetCampaignQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RechargeResetCampaignQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RechargeResetCampaignQuery) Clone() *RechargeResetCampaignQuery {
	if _q == nil {
		return nil
	}
	return &RechargeResetCampaignQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]rechargeresetcampaign.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.RechargeResetCampaign{}, _q.predicates...),
		withRules:   _q.withRules.Clone(),
		withRecords: _q.withRecords.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithRules tells the query-builder to eager-load the nodes that are connected to
// the "rules" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RechargeResetCampaignQuery) WithRules(opts ...func(*RechargeResetCampaignRuleQuery)) *RechargeResetCampaignQuery {
	query := (&RechargeResetCampaignRuleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRules = query
	return _q
}

// WithRecords tells the query-builder to eager-load the nodes that are connected to
// the "records" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RechargeResetCampaignQuery) WithRecords(opts ...func(*RechargeResetRecordQuery)) *RechargeResetCampaignQuery {
	query := (&RechargeResetRecordClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRecords = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RechargeResetCampaign.Query().
//		GroupBy(rechargeresetcampaign.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RechargeResetCampaignQuery) GroupBy(field string, fields ...string) *RechargeResetCampaignGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RechargeResetCampaignGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = rechargeresetcampaign.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.RechargeResetCampaign.Query().
//		Select(rechargeresetcampaign.FieldName).
//		Scan(ctx, &v)
func (_q *RechargeResetCampaignQuery) Select(fields ...string) *RechargeResetCampaignSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RechargeResetCampaignSelect{RechargeResetCampaignQuery: _q}
	sbuild.label = rechargeresetcampaign.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RechargeResetCampaignSelect configured with the given aggregations.
func (_q *RechargeResetCampaignQuery) Aggregate(fns ...AggregateFunc) *RechargeResetCampaignSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RechargeResetCampaignQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !rechargeresetcampaign.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RechargeResetCampaignQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RechargeResetCampaign, error) {
	var (
		nodes       = []*RechargeResetCampaign{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withRules != nil,
			_q.withRecords != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RechargeResetCampaign).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RechargeResetCampaign{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withRules; query != nil {
		if err := _q.loadRules(ctx, query, nodes,
			func(n *RechargeResetCampaign) { n.Edges.Rules = []*RechargeResetCampaignRule{} },
			func(n *RechargeResetCampaign, e *RechargeResetCampaignRule) { n.Edges.Rules = append(n.Edges.Rules, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRecords; query != nil {
		if err := _q.loadRecords(ctx, query, nodes,
			func(n *RechargeResetCampaign) { n.Edges.Records = []*RechargeResetRecord{} },
			func(n *RechargeResetCampaign, e *RechargeResetRecord) { n.Edges.Records = append(n.Edges.Records, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RechargeResetCampaignQuery) loadRules(ctx context.Context, query *RechargeResetCampaignRuleQuery, nodes []*RechargeResetCampaign, init func(*RechargeResetCampaign), assign func(*RechargeResetCampaign, *RechargeResetCampaignRule)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*RechargeResetCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rechargeresetcampaignrule.FieldCampaignID)
	}
	query.Where(predicate.RechargeResetCampaignRule(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(rechargeresetcampaign.RulesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CampaignID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "campaign_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RechargeResetCampaignQuery) loadRecords(ctx context.Context, query *RechargeResetRecordQuery, nodes []*RechargeResetCampaign, init func(*RechargeResetCampaign), assign func(*RechargeResetCampaign, *RechargeResetRecord)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*RechargeResetCampaign)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(rechargeresetrecord.FieldCampaignID)
	}
	query.Where(predicate.RechargeResetRecord(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(rechargeresetcampaign.RecordsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CampaignID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "campaign_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RechargeResetCampaignQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RechargeResetCampaignQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(rechargeresetcampaign.Table, rechargeresetcampaign.Columns, sqlgraph.NewFieldSpec(rechargeresetcampaign.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rechargeresetcampaign.FieldID)
		for i := range fields {
			if fields[i] != rechargeresetcampaign.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RechargeResetCampaignQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(rechargeresetcampaign.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = rechargeresetcampaign.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *RechargeResetCampaignQuery) ForUpdate(opts ...sql.LockOption) *RechargeResetCampaignQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *RechargeResetCampaignQuery) ForShare(opts ...sql.LockOption) *RechargeResetCampaignQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// RechargeResetCampaignGroupBy is the group-by builder for RechargeResetCampaign entities.
type RechargeResetCampaignGroupBy struct {
	selector
	build *RechargeResetCampaignQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RechargeResetCampaignGroupBy) Aggregate(fns ...AggregateFunc) *RechargeResetCampaignGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RechargeResetCampaignGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RechargeResetCampaignQuery, *RechargeResetCampaignGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RechargeResetCampaignGroupBy) sqlScan(ctx context.Context, root *RechargeResetCampaignQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RechargeResetCampaignSelect is the builder for selecting fields of RechargeResetCampaign entities.
type RechargeResetCampaignSelect struct {
	*RechargeResetCampaignQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RechargeResetCampaignSelect) Aggregate(fns ...AggregateFunc) *RechargeResetCampaignSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RechargeResetCampaignSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RechargeResetCampaignQuery, *RechargeResetCampaignSelect](ctx, _s.RechargeResetCampaignQuery, _s, _s.inters, v)
}

func (_s *RechargeResetCampaignSelect) sqlScan(ctx context.Context, root *RechargeResetCampaignQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
